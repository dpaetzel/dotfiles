# -*- mode: muttrc -*-


  set from      = "david.a.paetzel@gmail.com"
  set sendmail  = "/run/current-system/sw/bin/msmtp -a gmail"
unset mbox
unset record
  set postponed     = "+gmail/drafts"
  set signature     = "~/.signature"


macro index C "<change-folder>+dpaetzel/INBOX<enter>" "change accounts"


color status white default